@model MariosSpeciality.Models.Review

@{
    ViewData["Title"] = "Create";
    var productId = ViewBag.ProductId;
}

<h1>Create</h1>
@using (Html.BeginForm("Create", "Review", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <h2>Blog</h2><hr />
    <div class="form-horizontal">
        <div class="form-group">
            <input type="hidden" name="ProductId" value="@ViewBag.ProductId" />
             @Html.LabelFor(model => model.Author, new { @class = "col-md-2 label-control" })
            <div class="col-md-6">
                @Html.TextBoxFor(model => model.Author, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.ContntBody, new { @class = "col-md-2 label-control" })
            <div class="col-md-6">
                @Html.TextAreaFor(model => model.ContntBody, new { @class = "form-control", @rows = 10 })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.AuthorImg, new { @class = "col-md-2 label-control" })
            <div class="col-md-6">
                <input type="file" id="files" name="files" />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-2"></div>
            <div class="col-md-6">
                <input type="submit" value="Create" class="btn btn-lg btn-primary" />
            </div>
        </div>
    </div>
}
<div>
    <a href="@Url.Action("RedirectToProduct", new { productId = productId } )" class="btn btn-lg btn-default">
        <i class="fa fa-2x fa-arrow-circle-left" aria-hidden="true"></i>
        <span>Back to product</span>
    </a>
</div>

}
